export enum COMMANDS {
  COMMIT = "auto-gist.commit",
  BUMP_VERSION = "auto-gist.bump",
  GIT_GRAPH = "auto-gist.git-graph",
}

export enum VIEWS {
  COMMIT = 'auto-gist-view.convention-commit',
  BUMP_VERSION = 'auto-gist-view.bump',
  GIT_GRAPH = 'auto-gist-view.git-graph'
}